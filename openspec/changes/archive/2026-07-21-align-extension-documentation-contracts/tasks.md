## 1. Align current documentation and contracts

- [x] 1.1 Update the accepted design record and canonical `core-model`/`cli-surface` specifications to match runtime SDK imports, `oauth2 | none`, supported Extension roots, and the implemented passive documentation projection with deferred consumers.
- [x] 1.2 Correct SYSTEM and focused registry, SDK, and Profile codemaps without changing runtime source or package manifests.
- [x] 1.3 Run focused stale-wording checks and validate the change artifacts strictly before broader verification.

## 2. Doctrine and final verification

- [x] 2.1 Promote the passive documentation boundary and no-current-consumer doctrine into the canonical `core-model` and `cli-surface` implementation sidecars.
- [x] 2.2 Run documentation/spec checks, strict all-change validation, proportionate repository CI, cartography/diff checks, and `openspec-verify-change`; resolve every critical or warning finding and obtain independent review.
