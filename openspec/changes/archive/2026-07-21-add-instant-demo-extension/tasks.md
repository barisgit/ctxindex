## 1. Useful deterministic demo corpus

- [x] 1.1 Add failing focused expectations for demo-owned identifiers, realistic synthetic fixture variety, strict typed fields, complete Resources, deterministic checkpointing, and zero provider egress
- [x] 1.2 Implement the polished providerless tender Profile, Adapter, Extension root, and synthetic fixtures
- [x] 1.3 Run the focused example unit tests and typecheck

## 2. Publishable package path

- [x] 2.1 Add failing package-entry and freshness coverage for a self-contained official demo package entry
- [x] 2.2 Add clean package/provenance metadata and the deterministic bundle build used by generic package acquisition
- [x] 2.3 Prove installation of the exact local packed artifact through Sync, search, and `get`; retain npm publication and anonymous acquisition as a Human checkpoint

## 3. Five-minute walkthrough and launch artifacts

- [x] 3.1 Author complete package and Extension documentation covering trust, synthetic data, installation, Source setup, Sync, search, typed fields, complete retrieval, reset, and troubleshooting
- [x] 3.2 Add website-ready concise copy and representative deterministic output with explicit placeholders only for generated values
- [x] 3.3 Validate the documentation tree and execute the exact walkthrough against isolated state

## 4. End-to-end proof

- [x] 4.1 Update the relocated compiled external-Extension e2e to exercise the full useful corpus through generic Sync, search, field filtering, and `get`
- [x] 4.2 Run focused Extension, direct-install, package, and compiled e2e gates

## 5. Doctrine and final verification

- [x] 5.1 Refresh affected codemaps and promote applicable doctrine into `openspec/specs/extension-loading/implementation.md`
- [x] 5.2 Run `bun run ci`, `bunx openspec validate --all --strict`, and the OpenSpec verification workflow
- [x] 5.3 Obtain an independent read-only review, address in-scope findings, and commit the complete branch
