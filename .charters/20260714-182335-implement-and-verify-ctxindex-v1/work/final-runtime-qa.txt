# Fresh-context V1 runtime QA

Date: 2026-07-15

A fresh-context black-box operator was forbidden from reading the implementation diff or credentials and ran only shipped CLI/test surfaces with bounded timeouts. No live-provider traffic ran.

Passed:

- Complete isolated two-Realm workflow e2e.
- SPEC §12 exit taxonomy e2e.
- Relocated compiled external tenders lifecycle.
- Relocated compiled embedded-skills lifecycle.
- D3 external TypeScript Extension relocation spike.
- `bun cli -- describe --json` and `bun cli -- skills list --json` without initialization or stdin; both returned parseable deterministic JSON.

Operator verdict: all requested black-box validations passed; no runtime warning or artifact required follow-up.
