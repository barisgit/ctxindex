ctxindex V1 Slice 4 verification — 2026-07-15

Automated gate
- Mocked Gmail CLI search/get integration: passed with loopback-only provider and token endpoints, sandboxed CTXINDEX_CONFIG_HOME/DATA_HOME/CACHE_HOME/STATE_HOME, stable Source-scoped message Refs, full hydration, and a byte-identical cached second get with no second format=full request.
- CLI argument ordering regression (`get --json <ref>` and `search --json --remote <query>`): passed.
- Gmail retrieval, core retrieval, ResourceStore merge/hydration matrix, planner, and CLI focused tests: passed.
- `bun run typecheck`: passed.
- `bun run lint`: passed.
- `bun test`: passed.
- `./scripts/spikes/d3-compiled-extension/run.sh`: passed on pinned Bun 1.3.14.
- `git diff --check`: passed.
- No live provider traffic was used in the automated lane.

Human checkpoint: bounded live Gmail read
- Persistent ignored sandbox: `.ctxindex/checkpoints/v1-context-access-layer/gmail-live-read/`.
- Explicit Realm: `gmail-live-read`.
- OAuth grant scope observed: `https://www.googleapis.com/auth/gmail.readonly`.
- Source `01KXK98X6GTY13431J5A28J12A` bound explicitly to the Realm and selected Grant.
- One bounded live provider search: query `newer_than:30d`, explicit Source, remote-only, limit 5; exit 0; five `communication.message` results; stable `ctx://01KXK98X6GTY13431J5A28J12A/message/<provider-id>` Refs; expected `truncated` warning at the bound.
- One live get: exact returned Ref; exit 0; `communication.message@1`; complete payload with providerMessageId and non-empty bodyText; hydration timestamp set; no warnings.
- No provider write occurred: the grant is Gmail read-only, Gmail search/retrieve operations issue GET requests only, and the Adapter exposes no mutation Action in Slice 4.
- Credentials, tokens, message subjects, sender addresses, and body contents are omitted from this artifact.
- Sandbox preserved for the later Gmail Draft checkpoint.
