# Public GitHub Provider

`github.public` uses `auth.none()` and permits only `api.github.com`. It is intentionally limited to public, unauthenticated API access; GitHub's shared unauthenticated rate limits apply.
