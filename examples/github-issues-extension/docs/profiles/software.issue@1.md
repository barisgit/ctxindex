# Software Issue Profile

`software.issue@1` represents a public repository issue with its stable number, title, optional body, state, label names, lifecycle timestamps, and public GitHub URL.

The searchable fields are `number`, `state`, `labels`, `createdAt`, `updatedAt`, and `closedAt`. Actor, assignee, milestone, reaction, and other unnecessary provider metadata are not retained.
